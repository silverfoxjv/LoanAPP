$(document).ready(function() {
    // console.log("ready!");

    // var content = $('#alertMessage');
    // if (content && content.text().length !== 0) {
    //     alert(content.text());
    // }

});